{
  "name": "spelling-mvp-react",
  "version": "1.0.0",
  "description": "A minimum viable product prototype of an app for students to practice spelling words, built using React.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel --watch src --out-dir dist --presets react-app/prod"
  },
  "author": "Michael Foland",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
